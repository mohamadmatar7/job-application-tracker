name: Keep Supabase Alive

on:
  workflow_dispatch:
  schedule:
    - cron: "17 7,19 * * *"

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Supabase
        run: |
          curl --fail --silent --show-error \
            "https://mohamad-applications.vercel.app/api/keep-alive"