name: Keep Supabase Alive

on:
  workflow_dispatch:
  schedule:
    - cron: "17 7,19 * * *"

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping application
        run: curl --fail "https://mohamad-applications.vercel.app/nl"